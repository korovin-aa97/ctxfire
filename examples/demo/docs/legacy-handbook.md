# Legacy handbook

This synthetic file represents an always-loaded handbook that the agent no
longer needs for ordinary work. The optimized configuration makes that removal
explicit instead of pretending that the file has no cost.

The content is intentionally small. Real repositories often repeat much larger
instruction collections across many scheduled agents.
